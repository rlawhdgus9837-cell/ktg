# 지인 공유용 데모 배포 방법

이 압축파일은 Supabase 가입 없이 바로 배포하는 시연용 버전입니다. 실제 고객의 주문이나 중요한 개인정보를 저장하는 용도로 사용하면 안 됩니다.

## GitHub에 올릴 파일

- `app.py`
- `demo_app.py`
- `requirements.txt`
- `.gitignore`

`ktg_demo.db` 파일은 올리지 않습니다.

## Streamlit 배포

1. GitHub에서 비공개 저장소를 하나 만듭니다.
2. 위 네 파일을 업로드합니다.
3. https://share.streamlit.io 에 로그인합니다.
4. `Create app`을 선택합니다.
5. GitHub 저장소와 `main` 브랜치를 선택합니다.
6. 실행 파일에는 `demo_app.py`를 입력합니다.
7. `Deploy`를 선택합니다.

배포가 끝나면 생성된 `streamlit.app` 주소를 지인에게 보내면 됩니다.

## 대표 계정

```text
아이디: ktg_demo_admin
비밀번호: KTGdemo!2026
```

대표 계정 정보는 지인에게 알려주지 마세요. 일반 회원 계정만 직접 가입해서 사용하게 하면 됩니다.

## 확인 순서

1. 본인 PC에서 일반 회원 계정을 가입합니다.
2. 휴대폰에서 배포 주소를 열고 같은 아이디로 로그인합니다.
3. 밀링·선반·기타 도면 발주를 시험합니다.
4. 대표 계정으로 로그인해 전체 발주와 답변 기능을 확인합니다.

## 데모판의 한계

같은 배포 서버가 유지되는 동안에는 다른 기기에서도 같은 계정과 발주를 볼 수 있습니다. 그러나 Streamlit 서버가 재배포되거나 저장공간이 초기화되면 계정과 발주가 사라질 수 있습니다. 실제 운영을 시작할 때는 함께 제공된 영구 저장 버전 `app.py`를 Supabase PostgreSQL에 연결해야 합니다.
