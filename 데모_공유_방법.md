# KTG 온라인 가공 견적 배포 방법

이 압축파일은 GitHub와 Streamlit Community Cloud에 바로 올려 지인에게 공유하는 시험 운영용 버전입니다.

## GitHub에 올릴 파일

- `app.py`
- `demo_app.py`
- `requirements.txt`
- `.gitignore`
- `.streamlit/config.toml`

`ktg_demo.db` 파일은 올리지 않습니다.

## Streamlit 배포

1. GitHub에서 비공개 저장소를 하나 만듭니다.
2. 위 네 파일을 업로드합니다.
3. https://share.streamlit.io 에 로그인합니다.
4. `Create app`을 선택합니다.
5. GitHub 저장소와 `main` 브랜치를 선택합니다.
6. 실행 파일에는 `demo_app.py`를 입력합니다.
7. `Deploy`를 선택합니다.

배포가 끝나면 생성된 `streamlit.app` 주소를 지인에게 보내면 됩니다.

## 대표 계정

```text
아이디: 대표
비밀번호: 1234
```

대표 계정 정보는 지인에게 알려주지 마세요. 일반 회원 계정만 직접 가입해서 사용하게 하면 됩니다.

## 확인 순서

1. 로그인하지 않은 상태에서 밀링 또는 선반 규격을 입력하고 예상 견적을 확인합니다.
2. 주문 요청을 누른 뒤 회원가입하면 입력해 둔 주문이 자동으로 접수되는지 확인합니다.
3. 휴대폰에서 같은 아이디로 로그인해 주문 내역이 보이는지 확인합니다.
4. 대표 계정으로 로그인해 전체 주문, 도면 답변, 회원 연락처와 가격 설정을 확인합니다.

## 데모판의 한계

같은 배포 서버가 유지되는 동안에는 다른 기기에서도 같은 계정과 주문을 볼 수 있습니다. 다만 이 시험 운영용 실행 파일은 서버 내부 SQLite를 사용하므로 Streamlit 서버가 재배포되거나 저장공간이 초기화되면 데이터가 사라질 수 있습니다. 실제 고객 주문을 받기 전에는 `app.py`를 Supabase PostgreSQL에 연결해 영구 저장 방식으로 전환해야 합니다.
