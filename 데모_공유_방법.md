# KTG 가공 견적 프로그램 · 배포 안내

이 압축파일에는 같은 화면을 사용하는 두 실행 파일이 있습니다. 실제 주문을 받을 때는 반드시 **`app.py`**를 실행하세요. `demo_app.py`는 지인 시연을 위한 별도 시험 모드입니다.

## GitHub에 올릴 파일

`app.py`, `demo_app.py`, `site.css`, `requirements.txt`, `.gitignore`, `.streamlit/config.toml`을 압축파일 안의 폴더 구조 그대로 올리세요. `ktg_demo.db`, `*.db`, 실제 비밀번호와 `secrets.toml`은 올리지 마세요.

## 실제 운영 · 기기가 달라도 주문 보존

1. Supabase 등에 PostgreSQL 데이터베이스를 생성하고 **Connect → Transaction pooler** 연결 문자열을 복사합니다.
2. Streamlit Community Cloud에서 GitHub 저장소와 브랜치를 고릅니다. 실행 파일은 **`app.py`**로 지정합니다.
3. 앱 설정의 **Secrets**에 아래 세 값을 실제 값으로 등록합니다. 예시의 괄호 문구는 실제 값을 입력해야 합니다. DB 연결 문자열이나 비밀번호를 GitHub에 올리지 마세요.

```toml
DATABASE_URL = "postgresql://실제-연결-문자열"
KTG_ADMIN_ID = "대표"
KTG_ADMIN_PASSWORD = "직접_정한_대표_비밀번호"
```

4. 먼저 PC에서 일반 고객 계정을 만들고 견적 요청을 접수합니다. 다른 기기에서 대표자 계정으로 로그인하여 주문·연락처·도면을 확인합니다. 대표자가 최종 견적을 입력하면 고객 계정에서 확인·수락하고 상태가 `결제 대기`로 바뀌는지 확인합니다.
5. 데이터베이스의 자동 백업과 실제 복구 방법을 준비합니다. **앱 코드만으로 데이터가 절대 유실되지 않는다고 보장할 수는 없습니다.** 도면도 주문과 함께 DB에 저장되므로 DB 백업 대상에 포함되는지 확인합니다.

기존 데모 DB에 실제 주문이 있다면 운영 DB로 바꾸기 전에 **별도 데이터 이전 작업**이 필요합니다. 실행 파일을 `app.py`로 변경해도 `ktg_demo.db`의 기록이 자동으로 옮겨지지 않습니다. 데모 DB 파일을 삭제하지 마세요.

## 지인 시연 전용 · `demo_app.py`

DB와 Secrets 설정 없이 빠르게 미리 보여줄 때 Streamlit 실행 파일을 `demo_app.py`로 지정합니다. 시연용 기본 대표 계정은 `대표` / `1234`입니다. 이 모드는 Streamlit 서버 내부의 `ktg_demo.db`에 저장합니다. 같은 배포 서버에 접속하는 다른 기기에서는 주문을 조회할 수 있지만, 서버가 재시작·재배포되거나 저장공간이 초기화되면 데이터 보존이 보장되지 않습니다. **실제 고객의 발주, 도면, 휴대폰 번호를 이 모드로 받지 마세요.**

## 주요 화면 확인

- 방문자: 밀링·선반 도면과 예상 금액을 로그인 없이 확인하고, 주문 요청 시 로그인·가입 창이 열리는지 확인합니다.
- 고객: 회사명·휴대폰 번호를 입력하고, 프로젝트·공차·후처리·희망 납기를 포함한 주문을 접수합니다. 기타 도면 첨부는 최대 10MB의 이미지·PDF·STEP·DXF·DWG·IGES를 받습니다.
- 대표자: 전체 고객 발주·연락처·첨부 파일·가격 기준을 확인하고 최종 금액·답변·납기·견적 유효기간을 입력합니다. 발주 목록 CSV를 내려받을 수 있습니다.
- 고객: 제시된 견적을 확인하면 `결제 대기`로 넘어갑니다. 앱에서 카드가 결제되지는 않으며 결제 방법은 별도로 안내해야 합니다.

공식 참고: [Streamlit Secrets 설정](https://docs.streamlit.io/deploy/streamlit-community-cloud/deploy-your-app/secrets-management), [Supabase 데이터베이스 연결](https://supabase.com/docs/guides/database/connecting-to-postgres), [Streamlit 저장 방식 주의](https://docs.streamlit.io/develop/concepts/connections/connecting-to-data)
